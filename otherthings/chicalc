#!/bin/sh
# chicalc — dmenu calculator using bc

expr=$(printf "" | dmenu -p "calc:")
[ -z "$expr" ] && exit

result=$(printf "scale=10; %s\n" "$expr" | bc -l 2>&1)

printf "%s" "$result" | dmenu -p "$expr ="
